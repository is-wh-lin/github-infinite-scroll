# GitHub API Configuration
# Get your personal access token from: https://github.com/settings/tokens
# Required scopes: public_repo (or no scopes for public repositories)
GITHUB_TOKEN=your_github_personal_access_token_here

# GitHub API Base URL (default: https://api.github.com)
# Can be overridden for GitHub Enterprise or testing
GITHUB_API_BASE_URL=https://api.github.com

# GitHub Pages Deployment Configuration
# Base URL for GitHub Pages deployment (e.g., /repository-name for subdirectory deployment)
# Leave empty or set to "/" for root domain deployment
NUXT_APP_BASE_URL=/github-infinite-scroll

# CDN URL for assets (usually same as base URL for GitHub Pages)
# Leave empty to use same domain as base URL
NUXT_APP_CDN_URL=

# API Rate Limiting Configuration
# Maximum requests per hour for GitHub API (5000 for authenticated requests)
GITHUB_API_RATE_LIMIT=5000

# Enable automatic rate limit handling (true/false)
ENABLE_RATE_LIMIT_HANDLING=true

# Environment (development/production)
NODE_ENV=development